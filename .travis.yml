language: bash
python:
    - "3.6"
    
install:
    - pip3 install -user pip wheel
    - pip3 install --user -r requirements.txt --progress-bar off

script:
    - export FILES="$(git diff --name-only $TRAVIS_COMMIT_RANGE)"
    - bash commands.sh
        
